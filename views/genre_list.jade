extends layout 

block content 
    h1= title
    if genres_list.length 
        ul 
            for genre in genres_list 
                li 
                    a(href=genre.url) #{genre.name}
    else 
        p There are no Genre